name: travelgpt
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - nodejs=20          # includes npm
  - pip:
      - fastapi
      - uvicorn
      - httpx
      - pydantic
      - python-dotenv
